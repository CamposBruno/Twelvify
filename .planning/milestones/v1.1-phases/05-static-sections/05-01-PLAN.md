---
phase: 05-static-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/index.html
  - landing/src/App.tsx
  - landing/src/constants.ts
  - landing/src/components/Nav.tsx
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, SEO-01, SEO-02, SEO-03]

must_haves:
  truths:
    - "Sticky nav bar renders with logo (bike icon + Twelveify), section links (HOW IT WORKS, FEATURES, PLAYGROUND), and ADD TO CHROME CTA button"
    - "On mobile (< md breakpoint), section links are hidden — only logo and CTA button visible"
    - "Page title, description, canonical, Open Graph, and Twitter Card meta tags are present in the built HTML"
    - "Semantic HTML landmarks are in place: <nav>, <header>, <main>, <footer> wrapping the right content"
    - "CHROME_STORE_URL constant is defined once and used in the Nav's CTA button"
  artifacts:
    - path: "landing/src/constants.ts"
      provides: "CHROME_STORE_URL constant (placeholder URL)"
      contains: "CHROME_STORE_URL"
    - path: "landing/src/components/Nav.tsx"
      provides: "Sticky nav component with logo, section links, CTA button"
      min_lines: 40
    - path: "landing/src/App.tsx"
      provides: "App shell with semantic layout and Nav mounted"
      contains: "Nav"
    - path: "landing/index.html"
      provides: "SEO meta tags — title, description, canonical, OG, Twitter Card"
      contains: "og:title"
  key_links:
    - from: "landing/src/constants.ts"
      to: "landing/src/components/Nav.tsx"
      via: "named import of CHROME_STORE_URL"
      pattern: "CHROME_STORE_URL"
    - from: "landing/src/components/Nav.tsx"
      to: "landing/src/App.tsx"
      via: "JSX import and render"
      pattern: "<Nav"
---

<objective>
Build the App shell, Nav component, Chrome Store URL constant, and all SEO meta tags.

Purpose: Establishes the layout scaffold (sticky nav + semantic page structure) and SEO foundation that all section components slot into in subsequent plans.
Output: Nav component, constants file, App shell with semantic HTML, and complete SEO meta tags in index.html.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-foundation/04-01-SUMMARY.md
@.planning/phases/04-foundation/04-02-SUMMARY.md
@landing/code.html
@landing/index.html
@landing/tailwind.config.ts
@landing/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants.ts and Nav component</name>
  <files>
    landing/src/constants.ts
    landing/src/components/Nav.tsx
  </files>
  <action>
    Create `landing/src/constants.ts`:
    - Export a single constant: `export const CHROME_STORE_URL = 'https://chrome.google.com/webstore';`
    - This is the placeholder used by all CTA buttons — easy to swap when extension goes live.

    Create `landing/src/components/Nav.tsx` — replicate the nav from code.html exactly:
    - Outer `<nav>` element: `sticky top-0 z-50 border-b-4 border-slate-900 bg-white`
    - Inner container: `max-w-7xl mx-auto px-6 lg:px-12 flex h-20 items-center justify-between`
    - Logo block (left): bike icon in rotated red box + "Twelveify" in font-display
      ```jsx
      <div className="flex items-center gap-3">
        <div className="bg-primary p-2 border-2 border-slate-900 rotate-[-4deg]">
          <span className="material-symbols-outlined text-white text-3xl">pedal_bike</span>
        </div>
        <span className="text-3xl font-display uppercase tracking-tighter text-slate-900">Twelveify</span>
      </div>
      ```
    - Section links (center-right): `hidden md:flex items-center gap-8 font-punk font-bold`
      - Three `<a>` anchor links pointing to section IDs:
        - `href="#how-it-works"` text "HOW IT WORKS" with `hover:bg-primary hover:text-white px-2 py-1 transition-all -rotate-1`
        - `href="#features"` text "FEATURES" with `hover:bg-primary hover:text-white px-2 py-1 transition-all rotate-2`
        - `href="#try-it"` text "PLAYGROUND" with `hover:bg-primary hover:text-white px-2 py-1 transition-all -rotate-1`
    - CTA button (right): `<a href={CHROME_STORE_URL} target="_blank" rel="noopener noreferrer">` wrapping a `<button>`:
      - Classes: `bg-slate-900 text-white font-punk border-2 border-slate-900 px-6 py-3 hover:bg-primary transition-all shadow-[4px_4px_0px_0px_rgba(245,96,96,1)]`
      - Text: "ADD TO CHROME"
    - Import `CHROME_STORE_URL` from `'../constants'`

    Mobile behavior: The `hidden md:flex` on the links div already hides section links on mobile. The logo and CTA button are always visible (per CONTEXT.md decision: "Mobile nav: logo + 'Add to Chrome' CTA button only").
  </action>
  <verify>
    Run `cd /Users/brunocampos/Twelvify/landing && npm run build` — must exit 0 with no TypeScript errors.
    Check that `landing/src/components/Nav.tsx` exists and contains `CHROME_STORE_URL`.
  </verify>
  <done>Nav component renders sticky nav with logo, section links (desktop only), and CTA button linked to CHROME_STORE_URL constant. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Update App.tsx shell + add SEO meta tags to index.html</name>
  <files>
    landing/src/App.tsx
    landing/index.html
  </files>
  <action>
    Replace `landing/src/App.tsx` with the page shell. Remove the design system demo content entirely. App.tsx should:
    - Import Nav from `'./components/Nav'`
    - Return semantic layout:
      ```tsx
      function App() {
        return (
          <>
            <Nav />
            <main>
              {/* Section components will be added in plans 02 and 03 */}
            </main>
            <footer id="footer">
              {/* Footer component will be added in plan 03 */}
            </footer>
          </>
        )
      }
      export default App
      ```
    - Keep the `<>` fragment wrapper (no extra div wrapper needed — body already has base classes in index.html)

    Update `landing/index.html` — add SEO meta tags inside `<head>`, after the existing `<title>` tag:
    - Title: `<title>Twelvify | Understand anything, anywhere.</title>` (already there, verify it's correct)
    - Description: `<meta name="description" content="The browser extension that translates high-brow nonsense into plain English. Highlight any confusing text, click the wand, and read the simplification instantly." />`
    - Canonical: `<link rel="canonical" href="https://twelvify.com" />`
    - Open Graph tags:
      ```html
      <meta property="og:type" content="website" />
      <meta property="og:url" content="https://twelvify.com" />
      <meta property="og:title" content="Twelvify | Understand anything, anywhere." />
      <meta property="og:description" content="The browser extension that translates high-brow nonsense into plain English. Highlight any confusing text, click the wand, and read the simplification instantly." />
      <meta property="og:image" content="https://twelvify.com/og-image.png" />
      ```
    - Twitter Card tags:
      ```html
      <meta name="twitter:card" content="summary_large_image" />
      <meta name="twitter:url" content="https://twelvify.com" />
      <meta name="twitter:title" content="Twelvify | Understand anything, anywhere." />
      <meta name="twitter:description" content="The browser extension that translates high-brow nonsense into plain English. Highlight any confusing text, click the wand, and read the simplification instantly." />
      <meta name="twitter:image" content="https://twelvify.com/og-image.png" />
      ```

    Note on semantic structure: The Nav component wraps its content in `<nav>`, App.tsx uses `<main>` and `<footer>` at the top level. This satisfies SEO-03 heading hierarchy + landmark requirements. Individual section components will carry proper `<section>` tags and heading hierarchy (h1 in hero, h2 for section headings).
  </action>
  <verify>
    Run `cd /Users/brunocampos/Twelvify/landing && npm run build` — must exit 0.
    Check `dist/index.html` contains `og:title` and `twitter:card` meta tags (grep confirms): `grep -c "og:title\|twitter:card" landing/dist/index.html`
  </verify>
  <done>App.tsx renders Nav inside semantic layout. index.html contains all SEO meta tags (description, canonical, OG, Twitter Card). Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` in landing/ exits 0 — TypeScript and Vite build pass
- `landing/src/constants.ts` exports `CHROME_STORE_URL`
- `landing/src/components/Nav.tsx` imports and uses `CHROME_STORE_URL`
- `landing/dist/index.html` contains: `og:title`, `og:description`, `twitter:card`, `canonical`
- `landing/src/App.tsx` imports `Nav` and renders it inside semantic `<nav>/<main>/<footer>` structure
</verification>

<success_criteria>
- Nav component implemented with sticky positioning, zine/punk styling matching code.html
- Section links hidden on mobile (< md), CTA button always visible
- CHROME_STORE_URL defined once in constants.ts, used in Nav
- All SEO meta tags present in built HTML
- App.tsx is the page shell — Nav mounted, main/footer placeholder for sections
- `npm run build` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-static-sections/05-01-SUMMARY.md`
</output>
