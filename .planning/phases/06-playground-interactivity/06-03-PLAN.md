---
phase: 06-playground-interactivity
plan: 03
type: execute
wave: 2
depends_on: [06-01, 06-02]
files_modified:
  - landing/src/App.tsx
autonomous: false
requirements: [SECT-03, INTX-01, INTX-04, INTX-05]

must_haves:
  truths:
    - "The landing page renders the live Playground section where the placeholder div used to be"
    - "Navigating to /#try-it smooth-scrolls to the Playground section (nav PLAYGROUND link works)"
    - "Clicking FIX THIS MESS in the browser calls /api/playground, streams text in with typing animation, and disables the button on success"
    - "Rate-limit and error toasts appear correctly in the browser"
    - "The full page build passes with zero TypeScript errors"
  artifacts:
    - path: "landing/src/App.tsx"
      provides: "Imports and renders Playground component in place of the placeholder section"
  key_links:
    - from: "landing/src/App.tsx"
      to: "landing/src/components/Playground.tsx"
      via: "import + JSX render"
      pattern: "import Playground"
---

<objective>
Wire the Playground component into App.tsx — replacing the Phase 5 placeholder — and verify the full interactive flow works end-to-end in the browser.

Purpose: The Playground component built in Plan 02 and the backend route built in Plan 01 need to be connected and user-verified. This plan does the final wiring (one-line code change) and then pauses for the human to exercise the live demo.

Output: Updated `landing/src/App.tsx` with Playground imported and rendered. Human-verified end-to-end demo.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-playground-interactivity/06-01-SUMMARY.md
@.planning/phases/06-playground-interactivity/06-02-SUMMARY.md
@landing/src/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace playground placeholder in App.tsx with Playground component</name>
  <files>landing/src/App.tsx</files>
  <action>
Edit `landing/src/App.tsx`:

1. Add import at top with other component imports:
   ```ts
   import Playground from './components/Playground'
   ```

2. Replace the placeholder section:
   ```tsx
   {/* REMOVE this entire block: */}
   {/* Playground section placeholder — implemented in Phase 6 */}
   <section id="try-it" className="py-32 bg-slate-100">
     <div className="max-w-4xl mx-auto px-6 text-center">
       <p className="font-punk text-slate-400 text-lg">Playground coming soon — Phase 6</p>
     </div>
   </section>

   {/* REPLACE with: */}
   <Playground />
   ```

The resulting App.tsx render tree should be:
```tsx
<Nav />
<main>
  <Hero />
  <HowItWorks />
  <Playground />
  <Features />
  <CallToAction />
</main>
<Footer />
```

Verify: `cd /Users/brunocampos/Twelvify/landing && npx tsc --noEmit && npm run build 2>&1 | tail -10`
Build must succeed with no TypeScript errors.
  </action>
  <verify>
    Run: `cd /Users/brunocampos/Twelvify/landing && npx tsc --noEmit 2>&1`
    Must exit 0.
    Run: `grep -n "Playground" /Users/brunocampos/Twelvify/landing/src/App.tsx`
    Must show `import Playground` and `<Playground />`.
    Run: `grep -n "coming soon" /Users/brunocampos/Twelvify/landing/src/App.tsx`
    Must return no output (placeholder is gone).
  </verify>
  <done>
    App.tsx imports and renders `<Playground />` where the placeholder was.
    TypeScript compiles clean. Vite build exits 0.
    Nav anchor `#try-it` now points to the real Playground section.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — end-to-end playground demo in browser</name>
  <action>
    Start both servers and exercise the full interactive demo flow:
    1. `cd /Users/brunocampos/Twelvify/backend && npm run dev` (requires OPENAI_API_KEY in backend/.env.local)
    2. `cd /Users/brunocampos/Twelvify/landing && npm run dev`
    3. If /api/playground returns 404, add a Vite proxy to `landing/vite.config.ts`:
       `server: { proxy: { '/api': 'http://localhost:3001' } }`
    Then exercise each verification step below.
  </action>
  <what-built>
    Phase 6 complete: live AI playground demo wired end-to-end.
    - Backend `/api/playground` endpoint streams simplified text via SSE with 60/min rate limit
    - Playground React component with typing animation, one-shot disable, and toast error handling
    - All integrated into the landing page at /#try-it
  </what-built>
  <how-to-verify>
    1. Open http://localhost:5173 in Chrome
    2. Click "PLAYGROUND" in the nav — should smooth-scroll to the playground section
    3. Verify the section shows heading "Is this even English?" with highlighted words (superfluous, sesquipedalian, intellectual vertigo)
    4. Click "FIX THIS MESS" — button should change to "FIXING..." with hourglass icon
    5. Watch the text area: original highlighted text should be replaced character-by-character by the AI simplified version
    6. After completion: button should read "FIXED!" and be grayed out/disabled
    7. The legend at the bottom should update (COMPLEX ORIGINAL struck through or dimmed, SIMPLIFIED VERSION highlighted)
    8. Refresh the page — playground resets to original state (no localStorage persistence)
    9. To test rate-limit error: trigger the endpoint rapidly (6+ requests quickly) and confirm a toast notification slides in with a friendly message that auto-dismisses after 5 seconds
  </how-to-verify>
  <verify>Human exercised all steps above and confirmed expected behavior</verify>
  <done>Live playground demo works end-to-end: typing animation, one-shot disable, reset on refresh, toast on rate-limit</done>
  <resume-signal>Type "approved" if the demo works end-to-end, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `grep -n "Playground" /Users/brunocampos/Twelvify/landing/src/App.tsx` shows import and JSX usage
- `grep -n "coming soon" /Users/brunocampos/Twelvify/landing/src/App.tsx` returns no results (placeholder removed)
- `cd /Users/brunocampos/Twelvify/landing && npx tsc --noEmit` exits 0
- Human verified: typing animation works, button disables after success, toast appears on rate limit
</verification>

<success_criteria>
- App.tsx renders `<Playground />` not the placeholder
- Full Vite build passes
- Human-approved: live demo works end-to-end in browser
  - Typing animation: characters appear one-by-one at ~35ms speed
  - One-shot: button disabled and shows "FIXED!" after completion
  - Reset on refresh: playground back to original state
  - Error handling: rate-limit toast is visible and auto-dismisses
</success_criteria>

<output>
After completion, create `.planning/phases/06-playground-interactivity/06-03-SUMMARY.md`
</output>
