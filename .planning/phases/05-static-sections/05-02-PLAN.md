---
phase: 05-static-sections
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - landing/src/components/Hero.tsx
  - landing/src/components/HowItWorks.tsx
autonomous: true
requirements: [SECT-01, SECT-02, INTX-03]

must_haves:
  truths:
    - "Hero section renders with badge, h1 tagline, description paragraph, two CTA buttons, social proof, and CSS-built browser mockup"
    - "Hero CTA buttons ('Install Twelveify', 'View Demo') are linked — Install uses CHROME_STORE_URL, View Demo scrolls to #try-it"
    - "Browser mockup is built with HTML/CSS (divs), not an image — matching code.html exactly"
    - "Browser mockup is hidden on mobile (lg:block or similar), text content visible on all screen sizes"
    - "How-it-works section renders with three zine-box step cards (01. Highlight, 02. Click Wand, 03. Read Simplified)"
    - "Cards have rotation transforms (-2deg, 3deg, -1deg) and hover:rotate-0 transition on each card"
  artifacts:
    - path: "landing/src/components/Hero.tsx"
      provides: "Hero section with badge, headline, CTAs, social proof, CSS browser mockup"
      min_lines: 80
    - path: "landing/src/components/HowItWorks.tsx"
      provides: "How-it-works section with 3 step cards"
      min_lines: 60
  key_links:
    - from: "landing/src/components/Hero.tsx"
      to: "landing/src/constants.ts"
      via: "import CHROME_STORE_URL"
      pattern: "CHROME_STORE_URL"
    - from: "landing/src/components/HowItWorks.tsx"
      to: "landing/index.html"
      via: "id='how-it-works' anchor for nav smooth scroll"
      pattern: "how-it-works"
---

<objective>
Build the Hero and How-it-works section components from code.html.

Purpose: These are the first two content sections visitors see. Hero establishes the value proposition and primary CTAs. How-it-works explains the 3-step workflow.
Output: Two React components matching code.html exactly, with correct responsive behavior and hover effects.
</objective>

<execution_context>
@./.claire/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-static-sections/05-CONTEXT.md
@.planning/phases/05-static-sections/05-01-SUMMARY.md
@landing/code.html
@landing/src/constants.ts
@landing/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero section component</name>
  <files>landing/src/components/Hero.tsx</files>
  <action>
    Create `landing/src/components/Hero.tsx` replicating the `<header>` block from code.html.

    Structure (from code.html lines 70–128):
    - Outer `<header>` with `relative pt-24 pb-32 overflow-hidden`
    - Inner grid: `max-w-7xl mx-auto px-6 lg:px-12 grid grid-cols-1 lg:grid-cols-2 gap-20 items-center`

    LEFT COLUMN — text content:
    - Badge div: `inline-block px-4 py-2 bg-yellow-300 text-slate-900 border-2 border-slate-900 font-punk font-bold rotate-[-2deg] mb-4` — text: "NOW IN THE WEB STORE (DUH)"
    - `<h1>` with `text-6xl lg:text-8xl font-display leading-[0.9] text-slate-900 -rotate-2`:
      ```tsx
      Stop pretending you <span className="bg-primary text-white px-4 inline-block transform rotate-3">understand</span> jargon.
      ```
    - Description `<p>`: `text-xl font-punk text-slate-700 max-w-lg leading-relaxed border-l-4 border-primary pl-6 py-2 rotate-1` — exact copy from code.html
    - CTA buttons row (`flex flex-col sm:flex-row gap-6`):
      - "Install Twelveify" → `<a href={CHROME_STORE_URL} target="_blank" rel="noopener noreferrer">` wrapping `<button>` with classes `bg-primary text-white border-4 border-slate-900 px-10 py-6 text-2xl font-display shadow-[10px_10px_0px_0px_rgba(0,0,0,1)] hover:translate-x-1 hover:translate-y-1 hover:shadow-none transition-all`
      - "View Demo" → `<a href="#try-it">` wrapping `<button>` with same sizing classes + `bg-white border-4 border-slate-900 shadow-[10px_10px_0px_0px_rgba(0,0,0,1)] hover:translate-x-1 hover:translate-y-1 hover:shadow-none transition-all rotate-1`
    - Social proof row:
      - Avatar circles: Three colored divs (NOT external image URLs — use colored circles per Claude's discretion): `size-12 border-2 border-slate-900 rounded-full` with bg-slate-400, bg-slate-500, bg-slate-600, each with different rotation
      - Text badge: `bg-slate-900 text-white px-2 py-1` — text: "JOINED BY 10K+ DAILY READERS"

    RIGHT COLUMN — CSS browser mockup (hidden on mobile, shown on lg+):
    - Wrapper: `relative lg:mt-0 mt-20` — do NOT hide on mobile with `hidden lg:block` since code.html shows it always. Per CONTEXT.md: "Hero on mobile: text only — hide the browser mockup on small screens". Use `hidden lg:block` on the right column div.
    - Inner mockup: `zine-box bg-white p-2 transform rotate-3 scale-105`
      - Browser chrome bar: `border-b-4 border-slate-900 px-4 py-3 flex items-center justify-between bg-slate-100`
        - Traffic lights: three `size-4 border-2 border-slate-900` divs with bg-red-500, bg-yellow-400, bg-green-500
        - URL bar text: `font-punk text-xs font-bold text-slate-500` — "WIKIPEDIA.RUIN/QUANTUM"
      - Content area: `p-8 space-y-6`
        - Skeleton line: `h-6 w-1/2 bg-slate-200`
        - Text lines + highlighted block with wand badge (exact structure from code.html lines 110–120)

    Import `CHROME_STORE_URL` from `'../constants'`.
    Export as default.
  </action>
  <verify>
    Run `cd /Users/brunocampos/Twelvify/landing && npm run build` — must exit 0.
    Check file exists: `ls -la landing/src/components/Hero.tsx`
    Check CHROME_STORE_URL import: `grep "CHROME_STORE_URL" landing/src/components/Hero.tsx`
  </verify>
  <done>Hero component builds without TypeScript errors. Contains badge, h1, description, two CTA buttons with correct hrefs, social proof with colored circle avatars, and CSS-built browser mockup hidden on mobile (hidden lg:block).</done>
</task>

<task type="auto">
  <name>Task 2: Build How-it-works section component</name>
  <files>landing/src/components/HowItWorks.tsx</files>
  <action>
    Create `landing/src/components/HowItWorks.tsx` replicating the `<section id="how-it-works">` block from code.html (lines 129–163).

    Structure:
    - Outer `<section id="how-it-works">`: `py-32 relative bg-white border-y-8 border-slate-900`
    - Inner container: `max-w-7xl mx-auto px-6 lg:px-12`
    - Section header block (mb-24):
      - `<h2>` text: "The "Don't Look Dumb" Workflow"
      - Subtitle `<p>`: `font-punk text-lg text-slate-500 max-w-xl bg-yellow-100 px-4 py-2 inline-block rotate-1` — text: "No apps to open, no tabs to switch. Twelveify lives within your workflow."
    - Three-card grid: `grid grid-cols-1 md:grid-cols-3 gap-16 relative`

    Card 1 — Highlight:
    - `zine-box p-8 rotate-[-2deg] bg-white hover:rotate-0 transition-transform relative`
    - Step number: `text-4xl font-display text-primary mb-4 underline decoration-slate-900 decoration-4` — "01."
    - `<h3>` text: "Highlight"
    - `<p>` text: "Highlight the word salad. You know the one. That sentence that makes your brain itch."
    - Background icon: `material-symbols-outlined absolute -top-6 -right-6 text-6xl text-slate-200 -z-10 rotate-12` — use `draw` icon (Claude's discretion — matches code.html)

    Card 2 — Click Wand:
    - `zine-box p-8 rotate-[3deg] bg-white hover:rotate-0 transition-transform translate-y-4 relative`
    - Step number: "02."
    - `<h3>` text: "Click Wand"
    - `<p>` text: "Tap the magic wand. Our AI starts sweating so you don't have to."
    - Background icon: `material-symbols-outlined absolute -bottom-6 -left-6 text-6xl text-slate-200 -z-10 -rotate-12` — use `auto_fix_high`

    Card 3 — Read Simplified:
    - `zine-box p-8 rotate-[-1deg] bg-white hover:rotate-0 transition-transform relative`
    - Step number: "03."
    - `<h3>` text: "Read Simplified"
    - `<p>` text: "Read like a normal person. The jargon disappears, replaced by words you actually use."

    The `hover:rotate-0 transition-transform` on each card implements INTX-03 (rotation resets on hover).

    Export as default.
  </action>
  <verify>
    Run `cd /Users/brunocampos/Twelvify/landing && npm run build` — must exit 0.
    Check file: `ls -la landing/src/components/HowItWorks.tsx`
    Check section id: `grep "how-it-works" landing/src/components/HowItWorks.tsx`
    Check hover class: `grep "hover:rotate-0" landing/src/components/HowItWorks.tsx`
  </verify>
  <done>HowItWorks component builds without errors. Contains section id="how-it-works" anchor, section header, and three zine-box cards with correct rotation transforms and hover:rotate-0 transition. Step copy matches code.html exactly.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 in landing/
- `landing/src/components/Hero.tsx` and `HowItWorks.tsx` both exist
- Hero contains `CHROME_STORE_URL` import and `hidden lg:block` on mockup column
- Hero has `href="#try-it"` on View Demo button
- HowItWorks has `id="how-it-works"` on the section element
- All three cards have `hover:rotate-0 transition-transform` classes
</verification>

<success_criteria>
- Hero component: badge, h1 tagline, description, two CTA buttons (correct hrefs), colored-circle avatars, CSS browser mockup (desktop only)
- HowItWorks component: three zine-box cards with step numbers, icons, rotation transforms, hover effect
- Both components build cleanly — TypeScript passes, no linting errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-static-sections/05-02-SUMMARY.md`
</output>
