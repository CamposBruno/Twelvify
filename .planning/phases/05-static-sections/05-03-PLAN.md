---
phase: 05-static-sections
plan: 03
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - landing/src/components/Features.tsx
  - landing/src/components/CallToAction.tsx
  - landing/src/components/Footer.tsx
autonomous: true
requirements: [SECT-04, SECT-05, SECT-06, INTX-03]

must_haves:
  truths:
    - "Features section renders with headline, description, three feature items with rotating icons (swap_horiz, shield_with_heart, palette), and a zine-box illustration placeholder"
    - "Feature icon boxes have rotation transforms and hover:rotate-0 transition matching code.html"
    - "CTA section renders with paper-tear clip-path, red background, headline, 'Free to use — Forever' badge, and two CTA buttons"
    - "CTA 'ADD TO CHROME' button links to CHROME_STORE_URL"
    - "Footer renders with logo, tagline, three link columns (Product, Resources, Legal), and social links — all hrefs are '#'"
  artifacts:
    - path: "landing/src/components/Features.tsx"
      provides: "Features section with 3 feature items and illustration"
      min_lines: 70
    - path: "landing/src/components/CallToAction.tsx"
      provides: "CTA section with paper-tear and install buttons"
      min_lines: 40
    - path: "landing/src/components/Footer.tsx"
      provides: "Footer with logo, columns, copyright, social links"
      min_lines: 60
  key_links:
    - from: "landing/src/components/CallToAction.tsx"
      to: "landing/src/constants.ts"
      via: "import CHROME_STORE_URL"
      pattern: "CHROME_STORE_URL"
    - from: "landing/src/components/Features.tsx"
      to: "landing/index.html"
      via: "id='features' anchor for nav smooth scroll"
      pattern: "features"
---

<objective>
Build the Features, CTA, and Footer section components from code.html.

Purpose: These three sections complete the bottom half of the landing page — feature showcase, final install conversion, and site footer.
Output: Three React components matching code.html exactly, with correct hover effects, paper-tear CTA, and placeholder footer links.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-static-sections/05-CONTEXT.md
@.planning/phases/05-static-sections/05-01-SUMMARY.md
@landing/code.html
@landing/src/constants.ts
@landing/tailwind.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Features section component</name>
  <files>landing/src/components/Features.tsx</files>
  <action>
    Create `landing/src/components/Features.tsx` replicating `<section id="features">` from code.html (lines 193–242).

    Structure:
    - Outer `<section id="features">`: `py-32 bg-white border-t-8 border-slate-900`
    - Inner container: `max-w-7xl mx-auto px-6 lg:px-12`
    - Two-column grid: `grid grid-cols-1 lg:grid-cols-2 gap-24 items-center`

    LEFT COLUMN — feature list:
    - `<h2>` with `text-5xl lg:text-6xl leading-tight` (h2 base styles apply -1deg rotation from @layer base):
      ```tsx
      Designed to feel like it's <span className="text-primary underline decoration-slate-900 decoration-8 underline-offset-8">not even there</span>.
      ```
    - Description `<p>`: `font-punk text-xl text-slate-600 max-w-lg border-r-4 border-slate-200 pr-6` — exact copy from code.html
    - Three feature items in `space-y-8 pt-6`:

      Feature 1 — In-page Replacement:
      - Row: `flex gap-6 items-start group`
      - Icon box: `bg-slate-900 text-white p-3 rotate-12 group-hover:rotate-0 transition-transform`
      - Icon: `material-symbols-outlined text-2xl` — `swap_horiz`
      - `<h4>` with `text-2xl font-display mb-2` — "In-page Replacement"
      - `<p>` with `font-punk text-slate-500` — exact copy from code.html

      Feature 2 — Privacy by Design:
      - Icon box: `bg-primary text-white p-3 -rotate-6 group-hover:rotate-0 transition-transform`
      - Icon: `shield_with_heart`
      - `<h4>` — "Privacy by Design"
      - `<p>` — exact copy

      Feature 3 — Native Feel:
      - Icon box: `bg-slate-900 text-white p-3 rotate-3 group-hover:rotate-0 transition-transform`
      - Icon: `palette`
      - `<h4>` — "Native Feel"
      - `<p>` — exact copy

    RIGHT COLUMN — illustration placeholder:
    - Wrapper: `relative group mt-16 lg:mt-0`
    - "SHH!" text watermark: `absolute -top-12 -left-12 text-9xl font-display opacity-10 pointer-events-none select-none -rotate-12`
    - Zine-box frame: `zine-box p-2 bg-white rotate-[-3deg] group-hover:rotate-0 transition-transform duration-500`
      - Instead of the external image from code.html, render a CSS placeholder div:
        `<div className="w-full h-64 bg-slate-100 border-2 border-slate-900 flex items-center justify-center"><span className="material-symbols-outlined text-8xl text-slate-300">image</span></div>`
      - (Using a CSS placeholder avoids external image dependency — Phase 7 can add real image)
    - "CLEAN AS HELL" badge: `absolute -bottom-10 -right-6 bg-yellow-300 border-2 border-slate-900 p-4 font-display text-2xl rotate-6 shadow-xl`

    Export as default.
  </action>
  <verify>
    Run `cd /Users/brunocampos/Twelvify/landing && npm run build` — must exit 0.
    Check file: `ls -la landing/src/components/Features.tsx`
    Check section id: `grep 'id="features"' landing/src/components/Features.tsx`
    Check hover effects: `grep "group-hover:rotate-0" landing/src/components/Features.tsx`
  </verify>
  <done>Features component builds without errors. Has id="features" section anchor, h2 headline with styled span, three feature items with rotating icon boxes (group-hover:rotate-0 transition), illustration placeholder. All feature copy matches code.html.</done>
</task>

<task type="auto">
  <name>Task 2: Build CTA section and Footer components</name>
  <files>
    landing/src/components/CallToAction.tsx
    landing/src/components/Footer.tsx
  </files>
  <action>
    Create `landing/src/components/CallToAction.tsx` replicating the CTA `<section>` from code.html (lines 243–266):
    - Outer `<section>`: `py-32 px-6`
    - Inner container: `max-w-7xl mx-auto`
    - Main box: `bg-primary border-8 border-slate-900 p-12 lg:p-24 text-center relative paper-tear`
      - `paper-tear` is the clip-path class defined in @layer base (from Phase 4)
    - Content div: `relative z-10 space-y-12`
      - `<h2>`: `text-5xl lg:text-7xl text-white max-w-3xl mx-auto leading-[0.9] -rotate-1` — "Ready to stop feeling like a toddler? Join the waitlist."
        - (h2 base styles add -1deg — the -rotate-1 class adds another degree of rotation on top)
      - Badge div: `font-punk bg-white text-slate-900 px-6 py-2 inline-block font-black text-lg rotate-2 uppercase tracking-widest border-2 border-slate-900` — text: "Free to use — Forever"
      - Buttons row (`flex flex-col sm:flex-row items-center justify-center gap-8`):
        - "ADD TO CHROME" → `<a href={CHROME_STORE_URL} target="_blank" rel="noopener noreferrer">` wrapping `<button>` with `bg-slate-900 text-white px-12 py-6 text-3xl font-display border-4 border-white shadow-[10px_10px_0px_0px_rgba(255,255,255,0.3)] hover:translate-x-1 hover:translate-y-1 hover:shadow-none transition-all`
        - "LEARN MORE" → `<a href="#how-it-works">` wrapping `<button>` with `bg-transparent border-4 border-white text-white px-12 py-6 text-3xl font-display hover:bg-white hover:text-primary transition-all rotate-[-1deg]`
    - Decorative text: `!` top-right, `?` bottom-left — both with `absolute opacity-20 text-9xl font-display text-white select-none`
    - Import `CHROME_STORE_URL` from `'../constants'`

    Create `landing/src/components/Footer.tsx` replicating the `<footer>` from code.html (lines 267–318):
    - Outer `<footer>`: `bg-slate-900 text-white py-24 border-t-8 border-primary overflow-hidden`
    - Inner container: `max-w-7xl mx-auto px-6 lg:px-12`
    - Top row (`flex flex-col md:flex-row justify-between gap-16 mb-20 relative`):
      - Brand column: logo (bike icon in rotated box + "Twelveify" text) + tagline in font-punk italic
      - Link columns grid (`grid grid-cols-2 sm:grid-cols-3 gap-16 font-punk font-bold uppercase text-sm`):
        - Product: `<h5>` "Product" in text-primary font-display + ul with Extension, Enterprise, Pricing — all `href="#"`
        - Resources: `<h5>` "Resources" + Documentation, API, Changelog — all `href="#"`
        - Legal: `<h5>` "Legal" + Privacy, Terms, Security — all `href="#"`
      - "ZINE" watermark: `absolute -right-20 -top-10 text-[10rem] font-display text-white/5 select-none rotate-12`
    - Bottom bar (`pt-12 border-t-4 border-slate-800 flex flex-col md:flex-row justify-between items-center gap-8 font-punk text-xs uppercase tracking-widest text-slate-500`):
      - Copyright: "© 2024 Twelveify Inc. — Deal with it."
      - Social links (flex gap-10): Twitter, GitHub, Discord — all `href="#"` with `hover:text-primary transition-colors underline decoration-2 underline-offset-4`

    Export both as default.
  </action>
  <verify>
    Run `cd /Users/brunocampos/Twelvify/landing && npm run build` — must exit 0.
    Check files exist: `ls -la landing/src/components/CallToAction.tsx landing/src/components/Footer.tsx`
    Check CTA has CHROME_STORE_URL: `grep "CHROME_STORE_URL" landing/src/components/CallToAction.tsx`
    Check footer has social links: `grep "Twitter\|GitHub" landing/src/components/Footer.tsx`
    Check paper-tear: `grep "paper-tear" landing/src/components/CallToAction.tsx`
  </verify>
  <done>CallToAction builds with paper-tear class, ADD TO CHROME button using CHROME_STORE_URL, and LEARN MORE scrolling to #how-it-works. Footer builds with brand column, three link columns (all href="#"), social links, and copyright. Both components TypeScript clean.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 in landing/
- `landing/src/components/Features.tsx` has `id="features"`, three feature items with `group-hover:rotate-0`
- `landing/src/components/CallToAction.tsx` has `paper-tear` class and imports `CHROME_STORE_URL`
- `landing/src/components/Footer.tsx` has three link columns all with `href="#"` and social links
</verification>

<success_criteria>
- Features: h2 headline, three rotating-icon feature rows (group-hover:rotate-0), CSS illustration placeholder
- CTA: paper-tear clip-path, red background, badge, two buttons with correct hrefs
- Footer: logo, tagline, three link columns with placeholder hrefs, social links, copyright
- All three build cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/05-static-sections/05-03-SUMMARY.md`
</output>
